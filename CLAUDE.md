# dog-care-agent — 판단 규칙과 기록

오케스트레이터 한 저장소. 서브에이전트 둘은 **남의 저장소**이고, 여기서 고치지 않는다.

| 저장소 | 무엇 | 여기서의 위치 |
| --- | --- | --- |
| [dog-behavior-rag](https://github.com/gayeoniee/dog-behavior-rag) | 행동 상담 RAG | MCP 서브에이전트 |
| [dog-skin-screening](https://github.com/gayeoniee/dog-skin-screening) | 피부 2단계 스크리닝 | MCP 서브에이전트 |

## 규칙

- **서브레포를 고쳐서 문제를 풀지 않는다.** 저쪽 두 개는 각자 완결된 포트폴리오다.
  여기서 필요한 게 있으면 MCP 서버(`mcp_servers/`)에서 감싸서 해결한다.
  저쪽을 고쳐야만 풀리는 문제라면 **고치지 말고 여기 적어두고 멈춘다.**

- **지켜져야만 하는 것은 `gates.py` 에 둔다. 프롬프트는 부탁이다.**
  프롬프트에 "절대" 라고 쓰면 지켜지는 줄 알게 되는데, 재 보면 안 지켜진다.
  프롬프트로 부탁한 것은 잴 수 없고, 게이트에 있는 것은 테스트가 잡는다.

- **게이트 트리거는 게이트를 더 엄하게만 만들 수 있다.** 키워드로 검사를 *켜는*
  건 안전하다(헛걸려도 한 번 더 도는 것뿐). 키워드로 *끄는* 건 안전하지 않다 —
  목록에 없는 단어 하나가 통과 구멍이 된다.

- **게이트가 걸리면 답을 고쳐 쓰지 않고 막는다.** 고쳐 쓰면 무엇이 걸렸는지가
  기록에서 사라진다. 두 번 걸리면 LLM 을 빼고 `_compose` 가 조립한다.

- **평가는 라우팅과 게이트를 따로 잰다.** 한 번에 재면 RAG 코퍼스가 약한 날
  라우팅 점수가 같이 떨어져서 무엇이 나빠졌는지 못 가린다.

- **라우팅은 3회 돌리고 전 회차를 적는다.** 같은 질문에 매번 같은 툴을 고르지
  않는 게 이 층의 성질이다. 한 번 재고 숫자를 믿으면 안 된다.

- **커밋 메시지는 한글 서술형이고 접두사가 없다.** 제목에는 무엇이 어떻게
  잘못돼 있었는지를 쓰고(`~던 것`), 본문에 왜 그렇게 고쳤는지와 무엇을 재봤는지를 쓴다.

- **`pip` 을 쓰지 않는다.** `uv` 다.

## 정한 것과 그 이유

### 서브에이전트를 HTTP 가 아니라 MCP stdio 로 붙였다

피부는 torch·timm 을, RAG 는 bge-m3·asyncpg 를 단다. 한 venv 에 몰아넣으면
torch 버전 하나 때문에 상담이 안 뜨는 날이 온다. stdio 는 **서버가 남의
프로세스**라, 각자 `uv run --project <레포>` 로 자기 venv 안에서 뜬다.
오케스트레이터는 `mcp` 하나만 안다.

곁들여 오는 것: **하나가 죽어도 나머지가 돈다.** `Subagents.__aenter__` 가
실패를 `failed` 에 담고 계속 간다. 한 프로세스면 import 하나로 둘 다 안 뜬다.

### 모델에게 6종 이름을 주지 않는다 (`_for_model`)

저쪽 응답 계약에는 "1등 병변" 필드가 없다 — holdout 에서 그 이름이 56.6% 틀려서
앱이 고를 수 없게 아예 뺐다. 그런데 `distribution`(6종 확률)은 계약에 남아 있다
(콘솔이 쓴다). 그걸 그대로 LLM 에게 주면 **모델이 1등을 고른다.** 필드를 뺀
의미가 문장에서 되살아난다.

그래서 MCP 툴이 모델에게 주는 뷰에서 6종 이름이 실린 자리를 **전부** 뺀다.
게이트 G1 은 마지막 그물이고, 첫 줄은 **애초에 안 보여주는 것**이다. 전체
계약은 trace 에 그대로 남는다 — 숨기는 게 아니라 모델에게만 안 주는 것이다.
되돌리려면 `SKIN_SHOW_DISTRIBUTION=1`.

처음에는 `distribution` 만 뺐는데 **`labels` 로 고스란히 새고 있었다.** 저쪽은
계열 네 줄 **전부에** 용어 풀이(`"비듬·각질·상피성잔고리·태선화·과다색소침착"`)를
같은 방식으로 실어 앱 "자세히 보기" 에 둔다 — 사람에게는 단정이 아니라 풀이다.
그런데 산문을 쓰는 LLM 은 그 뉘앙스를 못 지키고 하나를 고른다. 이제 중첩
구조 어디서든 떼어낸다.

### 계열 이름은 저쪽이 2026-09-10 에 보호자 말로 바꿨다

`미란·궤양` → `벗겨지거나 패인 상처`, `결절·종괴` → `깊거나 단단한 혹` 식이다.
덕분에 계열 이름이 **6종과 글자가 하나도 안 겹친다** — 게이트가 단순해졌다.
옛 이름을 들고 있던 동안 게이트는 **에러 없이 엉뚱한 것을 막고 있었다.**
`tests/test_vocab_sync.py` 가 서브레포가 곁에 있을 때 대조한다.

### RAG 의 답을 다시 쓰지 않는다

저쪽 `RagService` 의 값어치는 대부분 **포기할 줄 아는 것**에 있다 — 근거가 없으면
없다고 하고, 원인이 여러 갈래면 되묻는다(범위 밖 질문 거절 0/4 → 7/7).
청크만 받아다 여기서 다시 답을 쓰면 그 거절이 사라진다. 그래서 MCP 툴이 `answer`
와 `coverage` 를 같이 넘기고, 위층은 지우지 않고 얹기만 한다. G3 가 감시한다.

### 게이트 두 번이면 LLM 을 뺀다

`_compose` 가 서브에이전트의 문장만으로 답을 맞춘다. 피부 계약의
`headline`/`body`/`action` 과 RAG 의 `answer` 는 저쪽이 이미 사람에게 보여도
된다고 정한 문장이라, 조립 결과는 정의상 안전하다. 뻣뻣하지만 "막혔습니다" 보다
낫고, 무엇보다 **틀린 병변 이름보다 낫다.** 조립한 답도 똑같이 잰다 — 안전하다고
믿고 안 재면 조립 규칙이 틀린 날 아무도 모른다.

## 틀렸던 것

- **`anyOf` 가 400 을 낸다.** 파이썬 `list[float] | None` 이 JSON Schema 로 가면
  `anyOf: [{type: array}, {type: null}]` 이 되는데, Gemini 의 OpenAI 호환
  엔드포인트가 이걸 거절한다. **에러 메시지에 `anyOf` 라는 말이 안 나와서** 키나
  모델 이름을 의심하게 된다. `subagents._sanitize` 가 널 갈래를 떼어낸다.

- **빈 환경변수가 자식을 죽였다.** 우리 `.env` 의 `DATABASE_URL=`(빈 줄)이 MCP
  서버 프로세스로 그대로 넘어가, 저쪽 레포가 자기 `.env` 를 읽기도 전에 빈
  문자열로 덮여서 `Could not parse SQLAlchemy URL` 로 죽었다. **빈 값은
  "안 정했다" 는 뜻이지 "빈 문자열로 정했다" 가 아니다.** 이제 빼고 넘긴다.

- **한국어 활용형 하나가 게이트를 뚫었다.** `_SKIN_HINTS` 에 `"빨갛"` 만 넣어서
  **"배가 자꾸 빨개져요" 를 피부 질문으로 못 봤다** — G5 가 안 켜졌다.
  `evals/run.py gates` 가 잡았다. 어간이 바뀌는 말은 형태를 나열해야 한다.
  (`tests/test_gates.py` 의 활용형 회귀 테스트)

- **툴 결과가 글자로 와서 게이트가 눈을 감았다.** MCP 서버가 판정 JSON 을
  `structured_content` 가 아니라 텍스트로 보냈는데, `_facts` 가 dict 가 아니라고
  흘려보내 `facts.screening` 이 None 이 됐다. **G1·G2·G4 가 전부 꺼진 채로**
  답이 나갔고, 답은 멀쩡해 보여서 아무도 몰랐다. 이제 `Subagents.call` 이
  JSON 으로 읽어 본다 (`tests/test_loop.py`).

- **`stage2.group` 은 문자열이 아니라 dict 다.** `{"name", "prob", "percent",
  "confidence", "text", "labels", "feature", …}`. 문자열로 알고 `.replace()` 를
  불렀다가 실기기에서 `AttributeError` 로 죽었다 — **게이트가 죽으면 답이 안
  나가는 게 아니라 요청이 통째로 죽는다.** `vocab.group_name()` 이 양쪽을 받는다.

- **사진 경로와 가이드 프레임을 모델이 정하게 두면 안 된다.** 요청에 들어 있는
  사실이지 고를 것이 아니다. 2단계는 **네모의 크기**를 쓰기 때문에 지어낸
  네모는 조용히 판정을 바꾼다. `_execute(pinned=...)` 가 덮어쓴다.

- **mcp 2.x 에서 `FastMCP` 가 `MCPServer` 로 바뀌었다.** 클라이언트 쪽도
  `inputSchema` → `input_schema`, `isError` → `is_error` 로 스네이크케이스가 됐다.
  1.x 도 받게 양쪽을 본다.

## 가중치와 코퍼스는 어디 있나

- **피부 가중치**: 레포에 없다(`best.pt` 가 100MB 리밋을 넘는다).
  허깅페이스 `gayoniee/daengs-skin-screening-release@v1` — **DAENGS 개발 서버가
  쓰던 바로 그 릴리스**다. 서버 PC 가 아니라 HF 에 있어서 서버가 없어져도 돈다.
  2단계 3팔 앙상블. 팔 수가 다르면 MCP 서버가 기동 때 죽고, G4 가 응답마다 또 본다.
- **RAG 코퍼스**: 저쪽 레포에도 없다(저작물 원본은 커밋하지 않는다).
  `scripts/collect/fetch.py` 로 재수집한다. 보듬TV 자막의 **LLM 정제본만은
  재생산이 비싸다**(Gemini 할당량) — 있으면 `data/raw/*.refined.json` 을 복사한다.
